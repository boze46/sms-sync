# 配置文件示例

该文件展示了 `config.json` 的各种配置选项和使用场景。

## 基础配置（当前默认）

```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{4,}",
  "enableNotification": true,
  "enableClipboard": true
}
```

**说明**：
- 监听 46000 端口
- 提取 4 位以上数字
- 自动复制到剪贴板
- 显示系统通知

---

## 配置选项详解

### 1. port（端口号）
- **类型**：数字
- **说明**：Socket 服务器监听端口
- **示例**：`46000`, `65432`, `8080`
- **建议**：使用 1024-65535 之间的端口

### 2. response（响应消息）
- **类型**：字符串
- **说明**：发送给 SmsForwarder 的响应内容（必须非空）
- **示例**：`"success"`, `"ok"`, `"received"`
- **建议**：保持默认 `"success"` 即可

### 3. regex（正则表达式）
- **类型**：字符串
- **说明**：用于提取验证码的正则表达式
- **注意**：使用双反斜杠转义（JSON 格式）

#### 常用正则表达式示例

| 场景 | 正则表达式 | 说明 |
|------|-----------|------|
| 4-8位数字 | `"\\d{4,8}"` | 匹配 4-8 位数字验证码 |
| 6位数字 | `"\\d{6}"` | 仅匹配 6 位数字 |
| 任意长度数字 | `"\\d+"` | 匹配任意长度数字 |
| 字母+数字 | `"[A-Za-z0-9]{6,}"` | 匹配 6 位以上字母数字组合 |
| 提取"验证码："后的数字 | `"验证码[：:](\\d+)"` | 提取"验证码："后的数字 |

### 4. enableNotification（是否显示通知）
- **类型**：布尔值
- **说明**：是否显示系统托盘通知
- **可选值**：`true`（启用）/ `false`（禁用）
- **建议**：日常使用建议启用，测试时可禁用

### 5. enableClipboard（是否复制到剪贴板）
- **类型**：布尔值
- **说明**：是否自动复制验证码到剪贴板
- **可选值**：`true`（启用）/ `false`（禁用）
- **建议**：通常启用以方便粘贴

---

## 使用场景配置示例

### 场景 1：仅提取，不复制不通知（静默模式）

```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{4,}",
  "enableNotification": false,
  "enableClipboard": false
}
```

**适用场景**：
- 仅需要在终端日志中查看验证码
- 不想被通知打扰
- 测试或调试

**输出示例**：
```
📱 收到来自 192.168.2.210 的连接
📨 接收数据: {验证码 123456}
🎯 提取到验证码: 123456
ℹ️  剪贴板功能已禁用
ℹ️  通知功能已禁用
```

---

### 场景 2：仅复制，不通知（安静模式）

```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{4,}",
  "enableNotification": false,
  "enableClipboard": true
}
```

**适用场景**：
- 自动复制到剪贴板
- 不想被通知声音打扰
- 工作时需要专注

**输出示例**：
```
📱 收到来自 192.168.2.210 的连接
📨 接收数据: {验证码 123456}
🎯 提取到验证码: 123456
✅ 已复制到剪贴板: 123456
ℹ️  通知功能已禁用
```

---

### 场景 3：仅通知，不复制（提醒模式）

```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{4,}",
  "enableNotification": true,
  "enableClipboard": false
}
```

**适用场景**：
- 需要手动输入验证码（更安全）
- 仅需通知提醒
- 剪贴板有其他重要内容

**输出示例**：
```
📱 收到来自 192.168.2.210 的连接
📨 接收数据: {验证码 123456}
🎯 提取到验证码: 123456
ℹ️  剪贴板功能已禁用
🔔 已发送通知
```

---

### 场景 4：提取 6 位数字验证码

```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{6}",
  "enableNotification": true,
  "enableClipboard": true
}
```

**适用场景**：
- 银行或支付应用（通常是 6 位）
- 严格限制验证码长度
- 避免误匹配

---

### 场景 5：提取字母+数字混合验证码

```json
{
  "port": 46000,
  "response": "success",
  "regex": "[A-Za-z0-9]{6,8}",
  "enableNotification": true,
  "enableClipboard": true
}
```

**适用场景**：
- 某些网站使用字母+数字混合验证码
- 示例：`A1B2C3`, `XYZ789`

---

### 场景 6：提取特定格式（如"验证码："后的数字）

```json
{
  "port": 46000,
  "response": "success",
  "regex": "(?<=验证码[：:])[0-9]+",
  "enableNotification": true,
  "enableClipboard": true
}
```

**适用场景**：
- 短信格式统一（如："您的验证码：123456"）
- 更精确的匹配，避免误提取

**注意**：使用了正则的后行断言（lookbehind），部分旧版本 JavaScript 引擎可能不支持。

---

## 测试配置

修改配置后，可以通过测试客户端验证：

```bash
# 启动服务器
bun start

# 新终端运行测试
bun test
```

观察输出，确认配置是否按预期工作。

---

## 常见问题

### 1. 正则表达式不工作？

**检查**：
- 确保使用双反斜杠 `\\` 转义
- JSON 格式正确（没有多余逗号）
- 正则表达式语法正确

**调试方法**：
```bash
# 查看服务器日志
bun start

# 如果提示正则错误：
# ❌ 正则表达式错误: ...
```

### 2. 修改配置后不生效？

**解决方法**：
- 重启服务器（Ctrl+C 停止，再运行 `bun start`）
- 检查 `config.json` 语法是否正确

### 3. 验证码没有被提取？

**检查**：
- 正则表达式是否匹配短信内容
- 查看服务器日志中的 "📨 接收数据" 和 "⚠️ 未找到符合正则"

**调试**：
```bash
# 使用测试客户端发送样例
bun test

# 观察输出，调整正则表达式
```

---

## 推荐配置

**日常使用（推荐）**：
```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{4,8}",
  "enableNotification": true,
  "enableClipboard": true
}
```

**工作场景（安静）**：
```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{4,8}",
  "enableNotification": false,
  "enableClipboard": true
}
```

**测试调试**：
```json
{
  "port": 46000,
  "response": "success",
  "regex": "\\d{4,}",
  "enableNotification": false,
  "enableClipboard": false
}
```
